#### 1. :exclamation: Dynamic Scoping is being deleted :exclamation:

---

#### THINGS THAT NEED TO BE INCLUDED IN THE DOCS:
1. How we handle variable in loop (currently `INT i` created in `!loop` cannot be changed inside loop block)

2. Operator overloads on `STRING`, `BOOL` etc. We need to explicitly specify which operators work on which types.